@model EntitiesAssignModel

    <form method="post"
          asp-controller="@nameof(ModerationController).GetController()"
          asp-action="@nameof(ModerationController.AssignEntitiesAsync)" class="my-2">
        @*<input type="hidden" asp-for="@Model.NumOfEntitiesToAssign" />
        <input type="hidden" asp-for="@Model.ModeratorId" />
        <input type="hidden" asp-for="@Model" />*@
        @*<input type="hidden" name="@nameof(Model.ModeratorId)" value="@Model.ModeratorId" />*@
        @*@Html.HiddenFor(m => m.ModeratorId);*@
        <input type="hidden" asp-for="@Model.ModeratorId" />

        <div class="row align-content-center">
            <div class="col my-auto">
                <span class="">Number of entities to assign</span>
            </div>
            <div class="col">
                <input class="form-control" asp-for="NumOfEntitiesToAssign" type="number" />
            </div>

            <div class="col">
                <button type="submit" style="width:100%" class="btn btn-primary">Assign</button>
            </div>
        </div>

        <div class="row">
            <div class="col">
                <span class="text-danger" asp-validation-for="NumOfEntitiesToAssign"></span>
            </div>
        </div>
    </form>